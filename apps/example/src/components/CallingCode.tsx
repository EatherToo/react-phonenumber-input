import { PhoneNumberCallingCodeProps } from 'react-phonenumber-text-input'

const CallingCode: React.FC<PhoneNumberCallingCodeProps> = (props) => {
  return (
    <div
      style={{
        width: '10px',
      }}
    ></div>
  )
}

export default CallingCode
